# 《矩阵的语言》写作约束

## 基本写作原则

本书始终以矩阵表达式的语义理解为主，不以运算技巧、形式证明或模型百科为主。解释顺序应尽量遵循：

```text
先语义，后公式
先轴，后形状
先元素展开，后抽象结论
先局部动作，后完整模型
```

正文要让读者知道一个表达式“在对谁做什么”，而不只知道它“怎么算”或“能不能相乘”。

## 统一阅读流程

核心表达式原则上按下列流程解释：

```text
1. 写出各矩阵形状
2. 标注每个轴的语义
3. 检查相乘维度的数量与语义是否同时匹配
4. 展开一个典型输出元素
5. 找出求和索引
6. 判断保留、汇总和新生成的维度
7. 翻译成自然语言
8. 必要时绘制信息流
```

“维度正确，不等于语义正确”是全书必须持续贯彻的判断标准。

## 统一术语

默认使用以下术语，并保持含义稳定：

- 对象轴：样本、用户、词元、节点等可被逐个表示或聚合的实体所在轴。
- 特征轴：属性、嵌入维度、隐藏维度等描述对象的维度。
- 关系矩阵：元素连接两个对象、两个特征或两个集合元素的矩阵。
- 关系轴：关系矩阵的某一轴；必须具体标明它代表哪一类对象或特征，不把“关系”本身视为抽象实体轴。
- 键对象或候选对象：在 `QK^T` 中与查询对象进行匹配的对象；只有其对应的 value 表示随后在 `AV` 中被读取时，才同时承担信息来源角色。
- 来源对象：在 `AX` 或 `AV` 中被求和、提供信息的对象。
- 目标对象：聚合后保留在输出中的对象。
- 特征变换：以 `XW` 为基本模式，对每个对象独立地重组特征。
- 关系构造：以 `XX^T`、`X^T X` 或 `QK^T` 为基本模式，生成对象或特征之间的关系。
- 信息聚合：以 `AX` 或 `AV` 为基本模式，按关系权重汇总来源对象的信息。
- 最佳近似：在模型可表达的集合中寻找最接近目标的表达。
- 低维结构：用少数主导方向或潜在因素解释复杂矩阵。

根据场景可以使用“样本”“用户”“词元”“节点”等具体词，但首次切换时必须明确它在通用框架中属于哪类对象。

## 核心表达式的稳定解释

以下解释跨章节保持一致：

```text
XW：沿旧特征轴求和；对象不混合；生成新特征。
AX：沿来源对象轴求和；特征轴保留；改变目标对象的信息来源。
XX^T：当 X 为“对象 × 特征”时，得到“对象 × 对象”关系。
X^T X：当 X 为“对象 × 特征”时，得到“特征 × 特征”关系。
QK^T：得到查询对象与键对象之间的匹配关系。
AV：查询或目标对象按照关系权重从 value 或来源对象读取信息。
```

可用以下对照反复强化，但避免无意义重复：

```text
XW 改变描述方式；AX 改变信息来源。
QK^T 构造关系；AV 使用关系聚合信息。
```

## 公式呈现

公式必须使用可直接阅读的 Markdown 形式。优先使用行内代码或代码块，例如：

```text
X: n × d
Y = XW
Y[i, r] = sum over j of X[i, j] * W[j, r]
```

在当前协作与草稿阶段，不依赖可能无法渲染的 LaTeX 数学环境。正式出版排版时可统一转换。

索引命名应体现语义，并在首次出现时说明。例如：

```text
i：目标对象或当前对象
p：来源对象
j：旧特征
r：新特征或输出维度
```

符号可按场景调整，但同一节和同一案例内必须保持一致。

## 文风与教学方式

正文应清晰、直接、克制，重视直觉但不牺牲准确性。避免用术语替代解释，也避免把同一句核心判断换词反复陈述。

每章应以一个清晰问题驱动，并优先使用少量贯穿案例反复应用同一阅读方法。案例服务于矩阵语义，不把章节写成应用罗列。

复杂公式必须拆成中间步骤，并分别说明各步的形状、轴语义和信息动作。第7章尤其遵循“不是介绍模型，而是解剖表达式”。

## 案例一致性

优先使用以下场景，并明确各轴：

```text
学生 × 课程：区分对象关系与特征关系，解释预测与潜在能力结构
用户 × 商品：解释跨集合匹配与潜在因素
词元 × 嵌入维度：解释自注意力
节点 × 特征：解释图关系与邻居聚合
```

不要求每章使用所有案例。每章选择最能服务核心问题的少量案例，避免频繁更换背景。

## 练习方向

练习逐步覆盖：公式转语义、轴标注、元素展开、错误诊断和语义转公式。前几章以阅读和诊断为主，后几章逐步增加组合阅读和自主设计。
